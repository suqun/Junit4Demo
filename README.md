# junit4 使用总结

  ```
  @Test
  
  @Test(timeout=毫秒)，@Test(expected=异常类)
  
  @Ignore
  
  @RunWith
  
  @BeforeCalss @AfterClass @Before @After
  
  测试套件@RunWith(Suite.class)
  
  参数化设置@RunWith(Parameterized.class)
  
  ```
